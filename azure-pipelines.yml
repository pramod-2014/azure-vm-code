trigger:
- none   # ya jo bhi branch hai

pool:
  name: Default   # jis pool me tera self-hosted agent hai

steps:
- task: AzureCLI@2
  inputs:
    azureSubscription: "onprem-k8s"   # tera service connection ka naam
    scriptType: bash
    scriptLocation: inlineScript
    inlineScript: |
      echo "Checking Azure connection..."
      az group create --name vmware-test-rg --location eastus
      echo "✅ Resource Group created successfully in Azure!"
