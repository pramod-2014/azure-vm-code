

trigger:
  - none

pool: 
  name: "Default"

steps:
- task: Kubernetes@1
  inputs:
    connectionType: 'Kubernetes Service Connection'
    kubernetesServiceEndpoint: 'OnPremKubeConnection'
    command: 'get'
    arguments: 'pods -A'
